# Testing

- Skip tests unless explicitly requested.
- Vitest is configured for unit tests in `apps/web-app`.
- Available commands (only when requested): `pnpm --filter @thom/web-app test`, `pnpm --filter @thom/web-app test:run`.
