# Workflow Rules

- Use `bd` for task tracking.
- Do not sign commit messages.
- Save work to `.idea/` in existing folders: `plans/`, `current-work/`, `improvements/`, `refs/`, `analysis/`.
- If a document does not fit, create a folder named after the content type (for example, `reviews/`).
- Make sure fixes do not mask the actual issue.
